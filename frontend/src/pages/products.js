import ProductList from "../components/ProductList";

function Products(){
  return (
    <div style={{padding:20}}>
      <h1>POS — Demo</h1>
      <ProductList />
    </div>
  );
}

export default Products;